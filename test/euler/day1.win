main := proc {
	n := 1000;

	sum := 0;
	for (i := 0; i < n; i = i + 1;) {
		if (i % 3 == 0) {
			sum = sum + i;
		}
		else {
			if (i % 5 == 0) {
				sum = sum + i;
			}
		}
	}

	print(sum);
};


main();