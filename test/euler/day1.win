main := proc {
	n : real = 1000;

	sum : real = 0;
	for (i : real = 0; i < n; i++) {
		if (i % 3 == 0) {
			sum = sum + i;
		}
		else if (i % 5 == 0) {
			sum = sum + i;
		}
	}

	print(sum);
};


main();