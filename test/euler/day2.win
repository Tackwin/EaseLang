main := proc {
	fib := proc (int x) -> int {
		if x < 2 {
			return x;
		}
		return fib(x - 1) + fib(x - 2);
	};

	sum := 0;
	f := 0;
	for (i := 0; fib(i) < 4000000; i = i + 1;) {
		f = fib(i);
		if (f % 2 == 0) {
			sum = sum + f;
		}
	}
	print(sum);
};
main();