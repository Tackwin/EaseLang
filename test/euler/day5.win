main := proc {
	n := 20;
	step := 2;
	step = step*3;
	step = step*5;
	step = step*7;
	step = step*11;
	step = step*13;
	step = step*17;
	step = step*19;

	flag := false;
	for (i := step; !flag; i = i + step) {
		flag = true;
		for (j := 1; j < n; j = j + 1) {
			if i % j == 0 {
			} else {
				flag = false;
			}
		}
		if (flag) {
		}
		print(i);
	}
};

main();